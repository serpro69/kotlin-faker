# START postgresql_provider_dict
en:
  faker:
    databases:
      postgresql:
        type:
          - INTEGER
          - BIGINT
          - DECIMAL
          - NUMERIC
          - REAL
          - DOUBLE PRECISION
          - SERIAL
          - BIGSERIAL
          - CHAR
          - VARCHAR
          - TEXT
          - DATE
          - TIMESTAMP
          - TIMESTAMP WITH TIME ZONE
          - BOOLEAN
          - JSON
          - JSONB
          - BYTEA
          - ARRAY
          - UUID
          - ENUM
# END postgresql_provider_dict