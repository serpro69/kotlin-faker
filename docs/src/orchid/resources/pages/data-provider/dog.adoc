---
---

== `Faker().dog`

.Dictionary file
[%collapsible]
====
[source,kotlin]
----
{% snippet 'provider_dog' %}
----
====

.Available Functions
[%collapsible]
====
[source,kotlin]
----
Faker().dog.name() // => Buddy

Faker().dog.breed() // => Affenpinscher

Faker().dog.sound() // => woof

Faker().dog.memePhrase() // => heck no pal

Faker().dog.age() // => puppy

Faker().dog.coatLength() // => hairless

Faker().dog.size() // => small
----
====
